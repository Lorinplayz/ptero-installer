#!/bin/bash
set -e

echo "🚀 Starting Teryx Panel installer By Lorinplayz..."

sudo apt update
sudo apt install -y curl git unzip

curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

cd ~
git clone https://github.com/Lorinplayz/YOUR-REPO.git panel || true
cd panel

npm install
npm run seed || true
npm run createUser || true

echo "✅ Installation complete"
echo "➡️ Start with: node ."
